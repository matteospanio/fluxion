<!-- generated by scripts/gen_interfaces.py — do not edit -->

# The op catalog

31 ops, generated from the single table in
[`crates/fluxion-core/src/op.rs`](../crates/fluxion-core/src/op.rs). The rule this table
exists to enforce is in [interfaces.md](interfaces.md): **an op ships on every interface,
or this document says why not.**

Every op is reachable from its name in the [chain text syntax](chain-syntax.md), which is
how C and JS reach the whole catalog without one symbol per op. Rust and Python spell the
same op as a function and a class respectively, both named from the row below.

| chain text / CLI | Rust | Python | C · JS |
|---|---|---|---|
| `gain` | `prelude::gain` | `fluxion.effect.Gain` | via chain text |
| `lowpass` | `prelude::lowpass` | `fluxion.filter.Lowpass` | via chain text |
| `highpass` | `prelude::highpass` | `fluxion.filter.Highpass` | via chain text |
| `normalize` | `prelude::normalize` | `fluxion.effect.Normalize` | via chain text |
| `peaking` | `prelude::peaking` | `fluxion.filter.Peaking` | via chain text |
| `lowshelf` | `prelude::lowshelf` | `fluxion.filter.LowShelf` | via chain text |
| `highshelf` | `prelude::highshelf` | `fluxion.filter.HighShelf` | via chain text |
| `notch` | `prelude::notch` | `fluxion.filter.Notch` | via chain text |
| `bandpass` | `prelude::bandpass` | `fluxion.filter.Bandpass` | via chain text |
| `allpass` | `prelude::allpass` | `fluxion.filter.Allpass` | via chain text |
| `delay` | `prelude::delay` | `fluxion.effect.Delay` | via chain text |
| `echo` | `prelude::echo` | `fluxion.effect.Echo` | via chain text |
| `cheby1_lowpass` | `prelude::cheby1_lowpass` | `fluxion.filter.Cheby1Lowpass` | via chain text |
| `cheby1_highpass` | `prelude::cheby1_highpass` | `fluxion.filter.Cheby1Highpass` | via chain text |
| `cheby2_lowpass` | `prelude::cheby2_lowpass` | `fluxion.filter.Cheby2Lowpass` | via chain text |
| `cheby2_highpass` | `prelude::cheby2_highpass` | `fluxion.filter.Cheby2Highpass` | via chain text |
| `reverb` | `prelude::reverb` | `fluxion.effect.Reverb` | via chain text |
| `fir` | `prelude::fir` | `fluxion.filter.Fir` | via chain text |
| `fade` | `prelude::fade` | `fluxion.effect.Fade` | via chain text |
| `tremolo` | `prelude::tremolo` | `fluxion.effect.Tremolo` | via chain text |
| `overdrive` | `prelude::overdrive` | `fluxion.effect.Overdrive` | via chain text |
| `compand` | `prelude::compand` | `fluxion.effect.Compand` | via chain text |
| `reverse` | `prelude::reverse` | `fluxion.effect.Reverse` | via chain text |
| `biquad` | `prelude::biquad` | `fluxion.filter.Biquad` | via chain text |
| `chorus` | `prelude::chorus` | `fluxion.effect.Chorus` | via chain text |
| `flanger` | `prelude::flanger` | `fluxion.effect.Flanger` | via chain text |
| `limiter` | `prelude::limiter` | `fluxion.effect.Limiter` | via chain text |
| `loudnorm` | `prelude::loudnorm` | `fluxion.effect.Loudnorm` | via chain text |
| `phaser` | `prelude::phaser` | `fluxion.effect.Phaser` | via chain text |
| `pitchshift` | `prelude::pitchshift` | `fluxion.effect.PitchShift` | via chain text |
| `gate` | `prelude::gate` | `fluxion.effect.Gate` | via chain text |

## What each op does

### Filters

**`lowpass(cutoff=1000, order=2)`** — Butterworth low-pass filter (`cutoff` Hz, integer `order`). The `lowpass` node.

> `cutoff` 0..∞ Hz · `order` 1..16

**`highpass(cutoff=1000, order=2)`** — Butterworth high-pass filter (`cutoff` Hz, integer `order`). The `highpass` node.

> `cutoff` 0..∞ Hz · `order` 1..16

**`peaking(frequency=1000, gain=0, q=0.707)`** — RBJ peaking EQ: `gain` dB around `frequency` with bandwidth `q`.

> `frequency` 0..∞ Hz · `gain` -∞..∞ dB · `q` 0.001..1000

**`lowshelf(frequency=1000, gain=0, q=0.707)`** — RBJ low shelf: `gain` dB below `frequency` (bandwidth `q`).

> `frequency` 0..∞ Hz · `gain` -∞..∞ dB · `q` 0.001..1000

**`highshelf(frequency=1000, gain=0, q=0.707)`** — RBJ high shelf: `gain` dB above `frequency` (bandwidth `q`).

> `frequency` 0..∞ Hz · `gain` -∞..∞ dB · `q` 0.001..1000

**`notch(frequency=1000, q=0.707)`** — RBJ notch at `frequency` with bandwidth `q`.

> `frequency` 0..∞ Hz · `q` 0.001..1000

**`bandpass(frequency=1000, q=0.707)`** — RBJ band-pass (0 dB peak) at `frequency` with bandwidth `q`.

> `frequency` 0..∞ Hz · `q` 0.001..1000

**`allpass(frequency=1000, q=0.707)`** — RBJ all-pass at `frequency` with bandwidth `q`.

> `frequency` 0..∞ Hz · `q` 0.001..1000

**`cheby1_lowpass(cutoff=1000, order=4, ripple=1)`** — Chebyshev Type I low-pass (`cutoff` Hz, `order`, passband `ripple` dB).

> `cutoff` 0..∞ Hz · `order` 1..16 · `ripple` 0.01..12 dB

**`cheby1_highpass(cutoff=1000, order=4, ripple=1)`** — Chebyshev Type I high-pass (`cutoff` Hz, `order`, passband `ripple` dB).

> `cutoff` 0..∞ Hz · `order` 1..16 · `ripple` 0.01..12 dB

**`cheby2_lowpass(cutoff=1000, order=4, atten=40)`** — Chebyshev Type II low-pass (`cutoff` = stopband edge Hz, `order`, stopband `atten` dB).

> `cutoff` 0..∞ Hz · `order` 1..16 · `atten` 10..120 dB

**`cheby2_highpass(cutoff=1000, order=4, atten=40)`** — Chebyshev Type II high-pass (`cutoff` = stopband edge Hz, `order`, stopband `atten` dB).

> `cutoff` 0..∞ Hz · `order` 1..16 · `atten` 10..120 dB

**`fir(tap=1, ...)`** — Direct-form FIR filter: `y[n] = Σ_k taps[k]·x[n-k]`. Variadic — its parameters are the tap vector itself (one or more), the realtime/graph form of a trained or frozen FIR. The one parameter listed is the prototype for a single tap.

> `tap` -∞..∞

**`biquad(b0=1, b1=0, b2=0, a1=0, a2=0)`** — A raw second-order section from explicit coefficients `b0 b1 b2 a1 a2` (`a0` normalized to 1). Reuses the biquad/SOS machinery, so it is differentiable / freezable / realtime like the designed filters.

> `b0` -∞..∞ · `b1` -∞..∞ · `b2` -∞..∞ · `a1` -∞..∞ · `a2` -∞..∞

### Effects

**`gain(gain=1)`** — Linear gain, `y = x * gain`.

> `gain` -∞..∞

**`normalize(peak=1)`** — Peak normalization to a target linear `peak`.

> `peak` 0..∞

**`delay(time=0.25, mix=0.5)`** — Single delayed tap crossfaded with the dry signal (`time` s, `mix`).

> `time` 0..60 s · `mix` 0..1

**`echo(time=0.25, feedback=0.3, wet=0.5)`** — Feedback echo: `wet` repeating echoes spaced `time` s apart with `feedback`.

> `time` 0..60 s · `feedback` 0..0.99 · `wet` 0..1

**`reverb(room=0.5, damping=0.3, mix=0.3)`** — Schroeder–Moorer reverb (`room` size, `damping`, wet/dry `mix`).

> `room` 0..1 · `damping` 0..1 · `mix` 0..1

**`fade(fadein=0, fadeout=0, shape=1)`** — Amplitude fade: `fadein` seconds ramping in, `fadeout` seconds ramping out, with a `shape` curve (0 = linear, 1 = quarter-sine [the SoX default], 2 = half-sine). Length-preserving.

> `fadein` 0..3600 s · `fadeout` 0..3600 s · `shape` 0..2

**`tremolo(rate=5, depth=0.5)`** — Tremolo: amplitude LFO at `rate` Hz dipping by `depth` (0..1). Length-preserving.

> `rate` 0..20000 Hz · `depth` 0..1

**`overdrive(gain=20, colour=0.2)`** — Overdrive: `gain` dB of drive through a `tanh` soft-clipper with a `colour` asymmetry bias. Nonlinear (not differentiable here).

> `gain` 0..100 dB · `colour` 0..1

**`compand(attack=0.01, release=0.1, threshold=-20, ratio=4, knee=6, makeup=0)`** — Feed-forward compressor / expander (compand): one-pole peak-envelope follower (`attack`, `release` s) driving a soft-knee gain computer (`threshold` dBFS, `ratio`, `knee` dB, `makeup` dB). Stateful per-channel — realtime-playable.

> `attack` 0..10 s · `release` 0..10 s · `threshold` -120..0 dB · `ratio` 1..100 · `knee` 0..48 dB · `makeup` -48..48 dB

**`reverse`** — Per-channel time reversal (no parameters). Length-preserving, but not realtime (it needs the whole signal).

**`chorus(rate=1.5, depth=0.002, delay=0.025, mix=0.5)`** — Chorus: an LFO-modulated fractional-delay voice (`rate` Hz, `depth` s, `delay` s) blended by `mix`. Feed-forward (no feedback). Length-preserving.

> `rate` 0..100 Hz · `depth` 0..1 s · `delay` 0..1 s · `mix` 0..1

**`flanger(rate=0.5, depth=0.002, delay=0.001, feedback=0.5, mix=0.5)`** — Flanger: a short LFO-modulated delay (`rate` Hz, `depth` s, `delay` s) with `feedback`, blended by `mix`. Length-preserving.

> `rate` 0..100 Hz · `depth` 0..1 s · `delay` 0..1 s · `feedback` -0.95..0.95 · `mix` 0..1

**`limiter(ceiling=-1, lookahead=0.005, release=0.05)`** — True-peak limiter: holds the reconstructed waveform under `ceiling` dBTP, looking `lookahead` seconds ahead so the gain is already down when a peak arrives and recovering over `release` seconds. Measures the oversampled curve, not the samples, so inter-sample overshoot — the kind that clips a converter or a lossy encoder — is caught too.

> `ceiling` -60..0 dB · `lookahead` 0.0001..0.1 s · `release` 0.001..1 s

**`loudnorm(target=-14, ceiling=-1)`** — Loudness normalize: bring the programme to `target` LUFS (ITU-R BS.1770), then hold its true peak under `ceiling` dBTP. Two passes — loudness is a property of the whole programme, so it has to be measured before anything can be decided. Material with enough crest factor cannot reach a loud target under a strict ceiling; it lands as close as the ceiling allows.

> `target` -70..0 dB · `ceiling` -60..0 dB

**`phaser(rate=0.5, depth=0.5, feedback=0.5, mix=0.5)`** — Phaser: an LFO-swept cascade of first-order all-pass stages (`rate` Hz, `depth`) with `feedback`, blended by `mix`. Length-preserving.

> `rate` 0..100 Hz · `depth` 0..1 · `feedback` -0.95..0.95 · `mix` 0..1

**`pitchshift(cents=0)`** — Pitch-shift by `cents`, keeping the length: 1200 cents is an octave up, -1200 an octave down. A phase vocoder stretches the material by the pitch ratio and a resampler plays it back that much faster, so the duration change cancels and the pitch change does not.

> `cents` -2400..2400

**`gate(threshold=-40, range=60, attack=0.001, hold=0.01, release=0.1)`** — Noise gate: below `threshold` the signal is turned down by `range` dB, opening over `attack` seconds, staying open for at least `hold` after the level drops, and closing over `release`. `range` is a reduction, not a mute, because a gate that slams to silence is more audible than the noise it removed. Takes a key (the `<` operator): with one, the gate listens to that signal and acts on this one — a different microphone opening this channel. Written without `<` it listens to itself, which is an ordinary noise gate. Written *with* `<` but handed no signal it hears silence and closes, which is the safe reading of a key that went missing.

> `threshold` -100..0 dB · `range` 0..120 dB · `attack` 0..1 s · `hold` 0..5 s · `release` 0..5 s

## Geometry stages (CLI only)

These change the frame count, the sample rate or the channel layout, so they cannot be
graph ops: the `|` / `+` algebra assumes every node preserves all three. They live in
`fluxion_ops::transform` and run between graph passes. Bringing them into the registry is
a design round of its own — see the 'Not yet' section of [interfaces.md](interfaces.md).

| stage | what it does | flags |
|---|---|---|
| `trim` | keep a [start, start+len) window (or [start, end)); drops the rest | `--start` (default 0), `--len` (default: to end), `--end` (alternative to --len) |
| `pad` | prepend/append silence | `--start` (default 0), `--end` (default 0) |
| `rate` | resample to a new sample rate (windowed-sinc SRC) | `--fs` (required) |
| `speed` | change speed (pitch+tempo together); keeps fs | `--factor` (required; >1 faster) |
| `stretch` | change tempo, not pitch; keeps fs | `--factor` (required; >1 faster) |
| `repeat` | concatenate the signal with itself count times | `--count` (required) |
| `silence` | trim leading/trailing near-silence | `--threshold-db` (default -60), `--min` (guard band, default 0), `--leading` (trim the front), `--trailing` (trim the tail) |
| `channels` | up/down-mix to count channels (energy-preserving) | `--count` (required) |
| `remix` | rebuild channels from a 1-based map, e.g. "1,2" or "1-2,3" | `--map` (required) |
