//! `fluxion-wasm` — WebAssembly / browser bindings (wasm-bindgen).
//!
//! Brings the functional graph API to the web: the CPU path runs as WASM, and the GPU path lowers
//! through the backend's WebGPU/WGSL target. Same graph algebra, no native
//! CUDA — the browser-portable slice of the cross-vendor story.
//!
//! Empty scaffold for now.
